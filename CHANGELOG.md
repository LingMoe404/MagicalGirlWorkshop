# 🚀 更新日志 (Changelog)

[← 返回主页 (Back to README)](README.md)

## v1.4.0 (2026-06-12)
*   ⚡ **多路文件转码**: QSV、NVENC、AMF 均支持同时处理多个视频；手动模式可固定为 1–4 路并发。
*   🧠 **自动动态并发**: 自动模式从 1 路开始，依据正式编码吞吐量、CPU、内存与硬件资源错误动态调整，最高 3 路；降低目标时不会中止已经运行的任务。
*   📈 **批次总体进度**: 顶部进度按视频时长加权汇总，探测与正式编码使用统一阶段进度，每个文件继续显示独立速度、ETA 与状态。
*   🧯 **错误队列与不中断补位**: 错误任务释放并发槽并按 FIFO 顺序等待处理，其他文件继续转码；30 秒无操作自动跳过，停止决定会终止整个批次。
*   🗂️ **隔离缓存与输出冲突保护**: 每个批次、每个文件使用独立临时目录；开工前检测多个输入写入同一目标的风险，避免并发任务互删缓存或覆盖输出。
*   🛡️ **远程桌面稳定性修复**: NVIDIA 硬件解码不再使用可能选中 DXVA2 的 `-hwaccel auto`，改为显式 CUDA 解码，修复 RDP 锁屏或断开后 Direct3D 设备创建失败导致的转码中止。
*   🔄 **按错误类型自动降级**: 硬件解码设备初始化失败时，仅对当前文件切换为 CPU 软件解码，编码仍使用 NVENC，CQ、预设、10-bit 与 AQ 等画质参数保持不变。
*   📝 **字幕错误准确识别**: 只有 FFmpeg 日志明确指向字幕流时才丢弃字幕，不再把硬件、权限或输出路径等未知错误误报为字幕损坏。
*   🧪 **三阶段重试保护**: 每个文件最多执行三次互不重复的尝试，支持“CUDA → CPU 解码”和“保留字幕 → 丢弃字幕”按实际错误顺序组合降级。
*   📊 **ab-av1 结果解析修复**: 不再把每个采样片段的 VMAF 当成完整 CRF 结果；每个 CRF 只显示一次汇总值，并要求退出码与正式成功标记一致。
*   ⚖️ **无解时优先保证画质**: 当目标 VMAF 与 ab-av1 默认 80% 体积限制无法同时满足时，自动选择达到目标 VMAF 的最大 CRF，并明确显示预测体积警告。
*   🛑 **禁止任意 CRF 开工**: 编码器、设备或输入异常，以及所有候选都低于目标 VMAF 时，不再强行采用最后样本或默认 CRF 24；当前文件会停止并保留真实错误。

## v1.3.0 (2026-05-27)
*   🌈 **HDR & 杜比视界**: 支持色调映射以及保留色调。
*   ⚙️ **“禁忌设定”深层参数面板**: 完整解锁 6 项底层运行参数调谐（核心探测超时、核心冷却、硬件解码加速、启动自动清理、后台并发线程和日志上限）。
*   🎨 **全局超清防虚化渲染**: 全局注入 `Microsoft YaHei UI` 微软雅黑 UI，强制 Qt 渲染引擎使用抗锯齿，完美修复高分屏发虚问题。
*   ⚖️ **字重轻量化重构**: 遵循 Win11 Fluent 原生美学，将设置面板及主配置卡片中过于臃肿的粗体字微调为常规字重，界面大幅美化。
*   🧹 **双重临时残渣清扫**: 实现了程序启动与压制工作线程生命周期终结时的双重垃圾缓存静默自动清理机制。
*   🌐 **多语言与一致性校验**: 补全中（简/繁）、英、日四国语言词条，并通过 `check_lang.py` 国际化对照同步检测。

## v1.2.1 (2026-02-17)
*   🛡️ **探测逻辑重构**: 为所有硬件编码器 (QSV/NVENC/AMF) 引入“三级探测策略” (硬件 -> SVT-AV1 -> AOM-AV1)。特别说明：由于 `ab-av1` 原生不支持 AMF，AMD 模式将强制使用 CPU (SVT-AV1 -> AOM-AV1) 进行探测并换算参数。
*   🎚️ **全域灵力偏移**: 开放“灵力偏移” (CRF Offset) 调节功能，支持针对 CPU 探测结果进行微调 (默认: QSV -2 / NVENC -4 / AMF -6)。
*   ⚡ **性能监控**: 任务列表新增实时压制速度 (Speed) 和剩余时间预估 (ETA) 显示；任务完成后常驻显示耗时统计。
*   🐛 **Bug 修复**: 修复了输入文件包含封面图 (MJPEG) 时导致 `ab-av1` 识别错误并崩溃的问题；修复了极短视频或多音轨视频导致的进度条计算异常；优化了 UI 列表布局。

## v1.2.0 (2026-02-16)
*   🔴 **新增 AMD AMF AV1 硬件编码支持**: 适配 Radeon RX 7000 系列及 RDNA 3 核显。
*   🔊 **音频增强**: 智能识别并保留 5.1/7.1 多声道 (Opus Multichannel)，新增响度标准化 (Loudnorm) 智能模式，避免破坏环绕声动态。
*   ⚡ **核心重构**: 底层框架迁移至 **PySide6**，引入异步任务队列与日志缓冲池，大幅提升大量文件列表下的 UI 响应速度。
*   🌈 **画质优化**: 全程强制 10-bit 像素处理 (AMD 为 8-bit)，优化 HDR 色彩保留；NVENC/QSV/AMF 均已启用感知增强 (AQ/Lookahead/PreAnalysis)。
*   🛠️ **体验升级**: 优化了任务进度条显示，增加剩余时间预估；修复了停止任务时后台进程残留的 Bug。

## v1.1.0 (2026-02-13)
*   💡 **新增 NVIDIA NVENC AV1 硬件编码支持** (需 RTX 40 系列)。
*   ⚙️ 优化硬件检测逻辑，区分显卡型号和驱动问题。
*   🚀 修复 `ab-av1` 参数兼容性问题，大幅提升 NVENC 压制画质 (开启 AQ 感知增强)。
*   📝 增加日志 VMAF 实时显示，NVENC 感知增强开关，以及 VMAF 默认值智能切换。

## v1.0.0 (2026-01-28)
*   🎉 首次发布！支持 Intel QSV AV1 硬件编码。
*   ✨ 集成 `ab-av1` 智能 VMAF 码率控制。
*   🎨 Win11 Fluent Design 风格界面。
